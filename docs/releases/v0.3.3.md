# video2timeline v0.3.3 Tech Preview

Local-first video-to-markdown packaging tool for LLM workflows.
This is a desktop-style local tool, not a hosted SaaS product.

## Baseline Support

- Windows
- Docker Desktop
- CPU mode

## Optional Features

- GPU mode: NVIDIA + Docker GPU access only, best-effort
- Speaker diarization: optional, requires both a Hugging Face token and gated approval for `pyannote/speaker-diarization-community-1`

## Download

- Windows: `video2timeline-windows-local.zip`
- macOS: source-based, experimental path

## What's New

- public-facing docs now match the current `v0.3.3` release line instead of older `v0.3.0` wording
- the release bundle ZIP now writes normalized `/` entry paths instead of Windows-style `\` separators
- release guidance and checklist wording were updated so future patch releases are less error-prone

## Known Limitations

- first run downloads models and takes time
- macOS is experimental
- GPU is not the baseline support path
- GUI is the primary supported path
- Docker Desktop is required

## Verification

- `dotnet build web/Video2Timeline.Web.csproj`
- `PYTHONPATH=worker/src python3 -m unittest discover worker/tests`
- `dotnet test tests/Video2Timeline.E2E/Video2Timeline.E2E.csproj`
- `scripts/build-release-bundle.ps1 -Version 0.3.3`
- GitHub Release asset download confirmed
